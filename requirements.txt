# Python dependencies for the brewery data pipeline
# Version pinning ensures reproducible builds across environments

# HTTP client for API requests
requests==2.31.0

# Data manipulation library (used in Bronze layer)
pandas==2.1.4

# Apache Arrow engine for Parquet format
pyarrow==14.0.2

# PySpark for distributed data processing (Silver and Gold layers)
pyspark==3.5.0

# Testing framework
pytest==7.4.3
